*.db
claude_desktop_config.json
whatsapp.log
# local per-machine MCP client config
.mcp.json

# compiled bridge binary
whatsapp-bridge/whatsapp-bridge

# binary from `go build ./...` (module name)
whatsapp-bridge/whatsapp-client
